{
  "name": "tabby-grouper",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Group related Chrome tabs with AI",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:icons": "node scripts/generate-icons.mjs",
    "zip": "cd dist && zip -r ../tabby-grouper.zip ."
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.25",
    "@types/chrome": "^0.0.268",
    "sharp": "^0.34.5",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  }
}
